<?xml version="1.0" encoding="UTF-8" ?>
<mapper namespace="local.test.mybatis.my.UserDao">
    <select id="get" parameterType="int" resultType="local.test.mybatis.my.User">
        select * from t_user where id = #{id}
    </select>
</mapper>
